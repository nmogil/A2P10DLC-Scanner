## What

Brief description of the change.

## Why

What problem does this solve?

## Testing

- [ ] `npm test` passes in `worker/`
- [ ] `npm run build` passes in `worker/`
- [ ] `npm run build` passes in `web/` (if frontend changes)
